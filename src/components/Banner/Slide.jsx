

const Slide = ({image}) => {
  return (
    
        <div
      className='w-full bg-cover h-[38rem]'
      style={{
        backgroundImage: `url(${image})`,
      }}
    >
      
    </div>
  )
}

export default Slide